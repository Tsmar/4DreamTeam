[project]
name = "fourdt-memory"
version = "0.1.0"
description = "Local 4DreamTeam memory storage tooling"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
